{
  "private": true,
  "scripts": {
    "repl": "npx spago -x dev.dhall repl",
    "example-Run": "npx spago -x dev.dhall build && (npx parcel serve --open -p 1337 --out-dir dev test/Test/Run/index.html & npx spago -x dev.dhall build --watch)",
    "example-TransformPath": "npx spago -x dev.dhall build && (npx parcel serve --open -p 1337 --out-dir dev test/Test/TransformPath/index.html & npx spago -x dev.dhall build --watch)"
  },
  "devDependencies": {
    "bower": "^1.8.8",
    "parcel": "^1.12.4",
    "pscid": "^2.9.3",
    "pulp": "^15.0.0",
    "purescript": "^0.13.8",
    "spago": "^0.17.0"
  }
}
