{ "private": true
, "scripts":
  { "example-run": "spago build && (parcel serve --open -p 1337 --out-dir dev test/Test/Run/index.html & spago build --watch)"
  , "example-transformPath": "spago build && (parcel serve --open -p 1337 --out-dir dev test/Test/TransformPath/index.html & spago build --watch)"
  }
}